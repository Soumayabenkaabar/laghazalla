import 'package:flutter/material.dart';
import 'package:flutter_adaptive_scaffold/flutter_adaptive_scaffold.dart';
import 'package:laghazala/profil.dart';

void main() {
  runApp(const ourtraining());
}

/// The main application widget for this example.
class ourtraining extends StatelessWidget {
  /// Creates a const main application widget.
  const ourtraining({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'jklm',
    );
  }
}
